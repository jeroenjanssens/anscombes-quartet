[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
polars = "^1.0.0"
plotnine = "^0.13.6"
jupyterlab = "^4.2.3"
scikit-learn = "^1.5.0"
pyarrow = "^16.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
